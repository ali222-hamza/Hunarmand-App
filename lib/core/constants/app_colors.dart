import 'package:flutter/material.dart';

// All colors used in Hunarmand app
class AppColors {
  // Primary blue used in buttons, headers, and cards
  static const Color primaryBlue = Color(0xFF1E3A8A);

  // Yellow/amber used in CTA buttons and highlights
  static const Color primaryYellow = Color(0xFFFBB700);

  // Light background color
  static const Color bgLight = Color(0xFFF8F9FA);

  // White
  static const Color white = Color(0xFFFFFFFF);

  // Dark text color
  static const Color textDark = Color(0xFF1A1A2E);

  // Grey text color
  static const Color textGrey = Color(0xFF6B7280);

  // Light grey for borders and dividers
  static const Color borderGrey = Color(0xFFE5E7EB);

  // Green for online indicator
  static const Color onlineGreen = Color(0xFF22C55E);

  // Red for SOS and errors
  static const Color danger = Color(0xFFEF4444);

  // Light blue background for cards
  static const Color cardBlue = Color(0xFFEFF6FF);

  // Verified badge color (gold/yellow)
  static const Color verifiedGold = Color(0xFFFBB700);

  // Gradient start color for splash
  static const Color gradientStart = Color(0xFFFFFBEB);

  // Gradient end color for splash
  static const Color gradientEnd = Color(0xFFEFF6FF);
}